spring:
  application:
    name: agentinsight
  config:
    import: optional:classpath:pricing.yml

server:
  port: ${SERVER_PORT:8081}

agentinsight:
  codex-source-path: ${AGENTINSIGHT_CODEX_HOME}
  data-path: ${AGENTINSIGHT_DATA_PATH}
  database-path: ${AGENTINSIGHT_DB_PATH}
